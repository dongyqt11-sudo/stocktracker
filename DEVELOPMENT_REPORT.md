# 开发报告

## 2026-05-26：持仓变化自动推算

### 完成内容

- 一致性校验支持自动计算相邻两次持仓快照的数量差。
- 没有成交记录但持仓数量发生变化时，不再标记为异常，而是返回 `inferred_changes`。
- Dashboard 增加蓝色“自动推算持仓变化”提示卡，可查看推算买入/卖出明细。

### 自测结果

- 后端编译检查通过。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。
- 当前账户校验结果从 3 条异常变为 0 条异常，并自动推算 3 条持仓变化：
  - 通信ETF：5000 股到 3400 股，推算卖出 1600 股。
  - 科创50：800 股到 2300 股，推算买入 1500 股。
  - 科创半导：5100 股到 5500 股，推算买入 400 股。

## 2026-05-25：优化与风险修复

### 完成内容

- 修复上传确认请求类型，成交页和资产页会按真实 `screenshot_type` 提交入库。
- 笔记增加账户字段和数据库迁移，列表、创建、编辑、删除均按当前账户隔离。
- 一致性校验从“全历史成交 vs 最新持仓”改为“相邻两次持仓快照 + 区间成交”校验，降低中途开始记录时的误报。
- 持仓截图确认入库前增加覆盖提示，避免误覆盖同账户同日期持仓快照。
- 交易流水 CSV 导出增加逗号、换行和引号转义。

### 自测结果

- 后端编译检查通过。
- 数据库迁移通过，`notes` 表已增加 `account_id`、`account_name`。
- 笔记账户隔离自测通过，账户 1 看不到账户 2 的测试笔记；测试数据已清理。
- 一致性校验接口自测通过，持仓快照不足两次时返回 0 条问题。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。

## 2026-05-22：阶段 2 第 6 步 数据一致性校验

### 完成内容

- 新增 `GET /api/dashboard/consistency` 接口，对比成交记录与最新持仓快照。
- 校验三种不一致类型：数量不一致（成交净买卖 vs 持仓数量）、持仓无成交记录、成交无持仓记录。
- Dashboard 首页新增黄色警告卡片：有不一致时自动展示，可展开查看每条详情的股票代码、名称、类型标签和差额。
- 校验仅提示，不自动修改用户数据。

### 自测结果

- 后端一致性模块导入通过。
- 一致性路由 `/api/dashboard/consistency` 已在应用注册。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。

## 2026-05-22：阶段 2 第 5 步 资产曲线分析页

### 完成内容

- 新增 AnalyticsPage，替代原占位页面。
- 大图（520px 高）展示资产曲线，总资产和持仓市值用面积图叠加，可用现金用折线。
- 支持近 7 日、30 日、90 日、半年、1 年及自定义天数切换。
- 自定义 Tooltip 在鼠标悬停时显示当天所有线的具体数值。
- 顶部汇总卡片展示总资产、持仓市值、可用现金、期间变动。

### 自测结果

- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。

## 2026-05-22：阶段 2 第 4 步 交易流水页

### 完成内容

- 新增 TransactionsPage，替代原占位页面。
- 表格列：时间、代码、名称、操作（买/卖带颜色标签）、数量、价格、金额、手续费。
- 顶部筛选：日期范围、股票代码搜索、买/卖下拉筛选。
- 支持导出 CSV（含 BOM，Excel 可直接打开）。
- 虚拟滚动：仅渲染可视区域内的行，支持大量数据流畅浏览。
- 顶部统计卡片：总笔数、买入笔数/金额、卖出笔数/金额、净买入。

### 自测结果

- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。

## 2026-05-22：阶段 2 第 3 步 Dashboard 首页 UI

### 完成内容

- Dashboard 改用 `/api/dashboard/summary` 数据源。
- 首页按参考图重做：四个资产数据卡片、资产曲线、持仓概览、最近交易、识别状态。
- 资产曲线支持近 7 日、近 30 日、近 90 日和自定义天数切换。
- 最近交易展示真实成交数据，持仓概览和最近交易的“查看全部”可跳转对应页面。
- 全局背景改为 `#FAFAFA`，卡片白底细阴影，移除旧渐变背景。

### 自测结果

- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。
- 本地服务重启后，前端首页和 Dashboard 汇总接口均返回 200。
- 使用 Chrome 无头模式渲染 Dashboard 截图，页面布局正常，无明显文本溢出或组件重叠。

## 2026-05-22：阶段 2 第 2 步 数据写入与 API

### 完成内容

- 确认入库支持三类截图：持仓写入 `holdings`，成交写入 `transactions`，资产写入 `assets_daily`。
- 成交和资产数据补齐账户维度，资产快照改为按“账户 + 日期”唯一，支持两个账户分别记录。
- 新增接口：`GET /api/transactions`、`GET /api/assets/timeline`、`GET /api/assets/latest`、`GET /api/dashboard/summary`。
- 上传页现在可确认成交页/资产页识别结果并写入本地数据库。
- 前端 API 客户端新增交易、资产、Dashboard 汇总调用函数。

### 自测结果

- 数据库自动迁移通过，`transactions` 和 `assets_daily` 已有账户字段。
- 直接写入自测通过：2 条成交、1 条资产快照均可确认入库；测试数据已清理。
- 新接口路由加载通过。
- 后端编译检查通过。
- 前端 TypeScript 类型检查通过。

## 2026-05-22：阶段 2 第 1 步 OCR 扩展

### 完成内容

- OCR 自动判断截图类型：持仓、成交、资产；无法判断时返回明确错误和原始 OCR 行。
- 成交页解析返回逐笔成交记录，包含时间、代码、名称、买卖方向、价格、数量、金额、手续费。
- 资产页解析返回总资产、持仓市值、可用现金、当日盈亏、累计盈亏，并对“总资产=持仓市值+可用现金”做差额提示。
- 上传页保持持仓可编辑确认，成交页/资产页先显示结构化 JSON 结果，供下一步接数据库前验 OCR。

### 自测结果

- 后端编译检查通过。
- 模拟 OCR 行测试通过：成交页可解析买入/卖出两条记录，资产页差额校验为 0。
- 后端应用导入检查通过。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。

## 2026-05-22：清理测试数据与股票代码补录

### 完成内容

- 新增清理脚本 `scripts/clean_test_data.py`，删除非 6 位数字股票代码的持仓记录，并同步清理关联截图和交易流水。
- 后端新增本地股票代码映射服务，上传识别后会从历史持仓和 `backend/data/stock_code_map.json` 自动建议股票代码。
- 确认入库时校验股票代码必须为 6 位数字，并自动更新本地映射文件。
- 前端上传页支持空代码补录、6 位代码校验、黄色待确认提示和蓝色“本地建议”标记。

### 自测结果

- 清理脚本实际清理：脏持仓 4 条、持仓记录 4 条、交易流水 0 条、截图记录 1 条。
- 清理后数据库持仓表记录数：0。
- 后端编译检查通过。
- 后端应用导入检查通过。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。
- 本地前后端服务已重启，前端首页和后端接口均返回 200。
